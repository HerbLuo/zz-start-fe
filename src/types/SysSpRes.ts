// 自动生成的代码块，首行
// 禁止修改 #HASH<2202f518a7bc919bf3ebb68a6a9072fd48c56e838df764bf68e0f07b02ec0b89>HASH#
// 如果该代码块被修改，下次执行生成操作时，则不会更新该代码块。
// 如果修改了代码块，且希望重新生成，可以删除这几行注释，该文件会强制重新生成。
import { SysSpEleEntity } from "./SysSpEleEntity";
import { SysSpEntity } from "./SysSpEntity";

export interface SysSpRes {
  /**  */
  elements: SysSpEleEntity[];
  /**  */
  query: SysSpEntity;
}
// 自动生成的代码块，尾行
