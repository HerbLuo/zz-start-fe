// 该文件由 ZZ-CODE-GEN 管理，不要尝试改变它。
// 该文件的 HASH值为 ###H#A#S#H###dd8ad1e9f752d3ae5a09b835c628fdba5e03cbad904556042c85d9195f6ac0f2###H#A#S#H###， 
// 如果你修改了该文件，下次执行"code gen"操作时，不会更新该文件
import { UsernamePassword } from "./UsernamePassword";

export interface Foo {
  b: boolean;
  bc: number;
  c: string;
  d: number;
  date: Date;
  date2: number;
  date3: string;
  date4: Date;
  f: number;
  i: number;
  l: string[];
  l2: number[];
  l3: any[];
  l4: boolean[][];
  l5: Array<Record<string, number>>;
  m: Record<string, string>;
  m2: Record<string, number>;
  m3: any;
  p0: Promise<string>;
  p1: Promise<number>;
  p2: Promise<boolean>;
  p3: Promise<string>;
  p4: Promise<Foo>;
  p5: Promise<Record<string, number>>;
  p6: Promise<Record<string, Foo>>;
  p7: Promise<Record<string, number[]>>;
  re: UsernamePassword;
  s: string;
}
