// 自动生成的代码块，首行
// 禁止修改 #HASH<d8afa3eca353f5d24d86b54df43f9da8a97b3acfb7de0584f958d4f1e10da881>HASH#
// 如果该代码块被修改，下次执行生成操作时，则不会更新该代码块。
// 如果修改了代码块，且希望重新生成，可以删除这几行注释，该文件会强制重新生成。
import { SysQueryElementEntity } from "./SysQueryElementEntity";
import { SysQueryEntity } from "./SysQueryEntity";

export interface SysQueryRes {
  /**  */
  elements: SysQueryElementEntity[];
  /**  */
  query: SysQueryEntity;
}
// 自动生成的代码块，尾行
