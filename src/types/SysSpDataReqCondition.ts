// 自动生成的代码块，首行
// 禁止修改 #HASH<3d4aae1f1b0b6c587ad287326df00443ee0f608570ba5a3965d15507f8bfc990>HASH#
// 如果该代码块被修改，下次执行生成操作时，则不会更新该代码块。
// 如果修改了代码块，且希望重新生成，可以删除这几行注释，该文件会强制重新生成。

export interface SysSpDataReqCondition {
  /**  */
  alias: string;
  /**  */
  conditions: SysSpDataReqCondition[];
  /**  */
  operator: string;
  /**  */
  value: unknown;
}
// 自动生成的代码块，尾行
